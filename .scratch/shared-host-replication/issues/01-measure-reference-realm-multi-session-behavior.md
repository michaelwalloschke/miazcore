# Measure Reference Realm multi-session behavior

Type: wayfinder:research
Status: open
Blocked by: None — can start immediately

## Question

What does the local Reference Realm actually permit and emit when the two
separate Fixture Pair accounts authenticate concurrently on loopback: duplicate
login constraints, Character visibility, initial proximity, clean logout, and
the server-side timing relevant to a deterministic Role-reversed Replication
Proof?

## Answer criteria

Record a redacted research asset using only the local Realm, owned fixtures,
and project tooling. It must distinguish observed fact from assumption, contain
no credentials or raw authenticated traffic, and identify every later decision
that depends on measured Realm behaviour.

